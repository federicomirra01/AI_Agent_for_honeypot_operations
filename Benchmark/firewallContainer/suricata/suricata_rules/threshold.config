
# Suricata Threshold Configuration
# File: rules/threshold.config

# Suppress the original overly broad reverse shell rule
#suppress gen_id 1, sig_id 2044751

# Optional: Add rate limiting for noisy rules
# threshold gen_id 1, sig_id 9000001, type limit, track by_src, count 5, seconds 60

# Optional: Suppress other noisy rules if needed
# suppress gen_id 1, sig_id XXXX
